<!-- ============================================================
     401 - YETKİSİZ ERİŞİM (Purpy Remastered Dark Luxury)
     ============================================================ -->
<div class="flex-grow flex items-center justify-center p-4 py-16 min-h-[60vh]">
  <div
    class="max-w-xl w-full rounded-3xl border-2 p-8 sm:p-12 shadow-2xl text-center backdrop-blur-xl relative overflow-hidden"
    style="
      background: linear-gradient(135deg, rgba(26, 16, 32, 0.95), rgba(18, 10, 24, 0.95));
      border-color: rgba(var(--color-primary-rgb, 124, 58, 237), 0.30);
    "
  >
    <div class="relative z-10">
      <div class="text-8xl sm:text-9xl font-black text-white/5 tracking-tighter mb-2 select-none">401</div>
      <h1 class="text-2xl sm:text-3xl font-black text-white tracking-tight mb-3 -mt-10 sm:-mt-14 relative z-10" style="color: #ffffff !important;">
        Giriş Yapılmadı
      </h1>
      <p class="text-xs sm:text-sm text-gray-300 font-medium mb-8 max-w-md mx-auto leading-relaxed">
        Bu sayfayı görüntülemek için giriş yapmanız gerekmektedir. Lütfen hesabınıza giriş yapın.
      </p>

      <div class="flex items-center justify-center gap-4">
        <a
          href="/auth/sign-in"
          class="px-8 py-3.5 rounded-2xl font-black text-xs uppercase tracking-wider text-white shadow-2xl transition-all duration-300 hover:scale-105 border border-white/20 flex items-center gap-2"
          style="
            background: linear-gradient(135deg, var(--color-primary), #9333ea);
            box-shadow: 0 10px 25px rgba(var(--color-primary-rgb, 124, 58, 237), 0.4);
          "
        >
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"/></svg>
          <span>Giriş Yap</span>
        </a>
      </div>
    </div>
  </div>
</div>
